1) In order to Compile the souce code enter following comand in into the termial: g++ -O bond1_proj1 -o bond1_proj1.cpp

2)To Run the excutable enter the following comand in into the termial: ./bond1_proj1

3) The program will ask the user to enter a password. Next it will read each character from the the input string and keep track of the upper case, lower case, digit, special characters and length; moving from q0, q1, q2 ... qf. At the end, the program ANDS all the requirements together to check that all requirements are met. if the input string meets all requirements then the string is accepted, if not then it is not accepted.


